# Minimal environment to SERVE the trained models (used for deployment).
# Deliberately excludes optuna, matplotlib, pyarrow — those are only
# needed for training, not for inference. Keeps the deployed image small
# and the cold-start fast on a free-tier host.
#
# Install with: pip install -r app/backend/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
PyYAML>=6.0
